enum Routes {
  main = "/",
  mangaList = "/manga",
  manga = "/manga/:id",
  gallery = "/manga/:id/gallery",
  games = "/games",
  game = "/game/:id",
  signUp = "/signUp",
  verification = "/verification",
  me = "/users/me",
  profileSettings = "/profile",
  videos = "/videos",
  singleVideo = "videos/:id",
}

export { Routes };
