const { dbUserService } = require("./db");
const crypto = require("crypto");

class UserService {
  initSelectUserData = async () => {};
}

const userService = new UserService();

module.exports = { userService };
